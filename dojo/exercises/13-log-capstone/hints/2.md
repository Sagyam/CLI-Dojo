To isolate the hour from timestamp `$4` (e.g. `[19/Aug/2026:03:15:22`):
```awk
split($4, a, ":")
hour = a[2]
```
Then aggregate 5xx errors:
```bash
awk '$9 >= 500 && $9 < 600 {split($4, a, ":"); errs[a[2]]++} END {for (h in errs) print h "\t" errs[h]}' access.log | sort > errors-by-hour.txt
```
