### Hints for Ticket 6 (Evidence Bundle)

To bundle the findings files into `evidence.tar.gz` with relative paths:

```bash
# Ensure you are in ~/dojo/20-finale
cd ~/dojo/20-finale

tar -czf evidence.tar.gz t3_rogue_file.txt t5_top3_ips.txt answers.txt
```

Verify your archive contents before running `dojo check 20`:
```bash
tar -ztvf evidence.tar.gz
```
