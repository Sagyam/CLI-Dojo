To extract only matching IP strings with regex from `auth.log`:
```bash
grep "Failed password" auth.log | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort -u | wc -l
```
The `-o` flag prints only the matching substring.
