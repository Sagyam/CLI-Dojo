For `solution.sh`, write a script accepting `$1` (the directory):
```bash
#!/usr/bin/env bash
target="${1:-.}"
grep -rn "nameserver" "$target" 2>/dev/null | grep -v "10.10.0.53" | cut -d: -f1
```
Make sure to `chmod +x solution.sh`.
