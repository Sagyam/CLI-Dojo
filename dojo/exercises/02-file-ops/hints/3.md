A hard link increases the inode's link count. When one link is deleted with `rm`, the file's data blocks remain on disk as long as at least 1 hard link still references the inode.
Check the link count using `stat -c %h config-backup.yaml`.
