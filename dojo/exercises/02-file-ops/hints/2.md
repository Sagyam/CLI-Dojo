To create symbolic links vs hard links:
- Symbolic link: `ln -s target linkname`
- Hard link: `ln target linkname`
After removing `config-2025.yaml`, check `cat config-current.yaml` vs `cat config-backup.yaml`.
