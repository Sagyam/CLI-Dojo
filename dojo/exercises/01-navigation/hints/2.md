To inspect file sizes in bytes without human formatting, use `stat -c %s <filepath>` or `ls -l`.
To determine the true format of `vpn-adapter.conf`, run:
```bash
file fake-root/etc/network/vpn-adapter.conf
```
Linux file extensions are just conventions; the `file` command reads header magic bytes.
