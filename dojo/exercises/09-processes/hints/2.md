`kill <PID>` sends signal 15 (`SIGTERM`).
`stubbornd` traps `SIGTERM`, so `kill -15` has no effect.
To forcefully kill a process that refuses to terminate:
```bash
kill -9 <PID>
```
Signal 9 (`SIGKILL`) is handled directly by the kernel and cannot be intercepted by the process.
