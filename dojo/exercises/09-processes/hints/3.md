To run a background process immune to terminal hangups:
```bash
nohup sleep 600 >/dev/null 2>&1 &
```
The special shell variable `$!` contains the Process ID of the most recently backgrounded job.
Write `$!` into `answers.txt` under `q4_nohup_sleep_pid:`.
