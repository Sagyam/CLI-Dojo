To extract all listening TCP port numbers and sort them ascending:

```bash
ss -tlnH | awk '{print $4}' | awk -F: '{print $NF}' | sort -un > ports.txt
```

To find the container's IP address:
```bash
ip -4 addr show eth0 | grep inet
# Or
hostname -I
```
