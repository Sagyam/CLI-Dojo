To inspect listening sockets and their process names:

```bash
# Using ss (socket statistics)
ss -tlnp

# Or filtering specifically for PORT_A
PORT_A=$(cat PORT_A.txt)
ss -tlnp | grep ":${PORT_A} "
```
Look inside the `users:(("...",pid,fd))` column for the process name.
