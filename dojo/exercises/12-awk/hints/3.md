For `solution.sh`, handle any passed log file path `$1`:
```bash
#!/usr/bin/env bash
INPUT="${1:-}"
if [[ -f "$INPUT" ]]; then
    awk 'NF>=3 {sum += $3; count++} END {if (count > 0) print int(sum/count)}' "$INPUT"
fi
```
Make sure `solution.sh` is executable (`chmod +x solution.sh`).
