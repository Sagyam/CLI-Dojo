To group and aggregate values in `awk`, use an associative array:
```bash
awk -F, 'NR>1 {plan[$3] += $4} END {for (p in plan) print p "\t" plan[p]}' usage.csv | sort > revenue.txt
```
The `-F,` flag splits on commas, `NR>1` skips the header, and `plan[$3] += $4` accumulates the sum by plan name.
