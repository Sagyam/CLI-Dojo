To calculate an average across rows using `awk`:
```bash
awk '{sum += $3} END {print int(sum/NR)}' pings.log
```
And to filter rows where latency is strictly greater than 100:
```bash
awk '$3 > 100 {count++} END {print count+0}' pings.log
```
