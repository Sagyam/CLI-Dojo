To sort by the 4th column numerically and in reverse (descending) order:
```bash
sort -t, -k4,4nr usage.csv
```
Use `head -n 5` to pick the top 5, and format with `awk -F, '{print $1 "\t" $4}'` or `cut` and `tr`.
