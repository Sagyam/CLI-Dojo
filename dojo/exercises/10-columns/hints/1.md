To extract columns from delimited files like CSV, use `cut` with `-d` (delimiter) and `-f` (field number):
```bash
tail -n +2 usage.csv | cut -d, -f3 | sort -u > plans.txt
```
The `tail -n +2` skips the CSV header line.
