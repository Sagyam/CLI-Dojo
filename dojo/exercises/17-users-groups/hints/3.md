To switch to Tara's account using her temporary password and generate the proof file:

```bash
# Check HR_NOTE.txt for the password, then run:
su - tara
# (Enter the password when prompted)

# Once in Tara's shell:
id > ~/proof.txt
exit
```
