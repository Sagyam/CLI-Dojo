To perform an in-place replacement while creating a `.bak` backup copy:
```bash
sed -i.bak 's/blr1\.yetilink\.internal\b/ktm2.yetilink.internal/g' configs/*.conf
```
Notice `\b` at the end of the hostname pattern to ensure near-misses like `blr1.yetilink.internal.backup` are not accidentally matched.
