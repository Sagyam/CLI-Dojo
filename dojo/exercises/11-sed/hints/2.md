To delete lines matching a regex in-place:
```bash
sed -i '/use_flannel/d' configs/*.conf
```
To comment out lines matching a pattern by prepending `# `:
```bash
sed -i '/deprecated_module/s/^/# /' nginx.conf
```
