To discard standard error and branch standard output to two files simultaneously:
```bash
./noisy-backup.sh 2>/dev/null | tee seen.log archive.log
```
The pipe `|` sends only stdout across the stream.
