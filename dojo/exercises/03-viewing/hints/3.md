You can also use `sed -n '/PANIC/=' core-router.log` to get the line number, and `sed -n '/PANIC/p' core-router.log` to print the line.
Remember to ensure `first5.txt` and `last5.txt` contain only 5 lines each.
