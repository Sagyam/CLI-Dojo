To search for the PANIC line and its line number at the same time:
```bash
grep -n "PANIC" core-router.log
```
The output format is `LINENUMBER:CONTENT`.
Save the content to `panic.txt` and the number to `answers.txt`.
