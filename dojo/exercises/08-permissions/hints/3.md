Umask determines the bits that are *disabled* (masked out) when creating files:
Base file creation mode is `666` (rw-rw-rw-).
To get `640` (rw-r-----), owner keeps `rw` (mask `0`), group loses `w` (mask `2`), others lose `rw` (mask `6` or `7`).
So standard umask is `027`.
