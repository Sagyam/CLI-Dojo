To extract only a specific file from a tarball, pass the exact relative path of the member as an argument to `tar -xzf`:

```bash
# Check contents.txt for the exact path inside the archive (e.g. etc/radius/radius.conf)
tar -xzf vault/backup-*.tar.gz etc/radius/radius.conf
```
