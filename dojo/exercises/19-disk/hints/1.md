To find directory sizes and sort them largest first:

```bash
# List direct subdirectories under disk-arena and sort human-readable
du -h --max-depth=1 disk-arena | sort -hr
```
Extract the 3 largest directory names (excluding the parent `disk-arena`) and write them to `top3-dirs.txt`.
